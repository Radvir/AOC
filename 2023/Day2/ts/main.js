const fs = require("fs");

let input = fs.readFileSync('input.txt', 'utf-8').split("\n");
// console.log(data);
let data;
for (let i = 0; i < input.length; i++) {
    let line_split = input[i].split(";");
    let temp_data;
    for (let i = 0; i < line_split.length; i++) {
        temp_data.Add()
    }
}
function part1(list) {
    
}

function part2(list) {
    
}

// part1(data);
part2(data);